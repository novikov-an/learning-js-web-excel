import {DomListener} from 'core/DomListener';

export class ExcelComponent extends DomListener {
  /**
   * Return HTML markdown of the element
   * @return {string}
   */
  toHtml() {
    return '';
  }
}
